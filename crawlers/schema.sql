CREATE TABLE prices (
    quote INTEGER,
    opening_date TEXT,
    open_price REAL,
    high_price REAL,
    low_price REAL,
    close_price REAL,
    close_adjust REAL,
    volume INTEGER
)

CREATE TABLE prices (
    `銘柄` INTEGER,
    `日付` TEXT,
    `始値` REAL,
    `高値` REAL,
    `安値` REAL,
    `終値` REAL,
    `終値調整` REAL,
    `売買高` INTEGER
)